server:
  port: 8040
spring:
  application:
    name: gateway-zuul
#注册中心地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://root:root@localhost:8761/eureka/

zuul:
  routes:
    user-server: /server/**